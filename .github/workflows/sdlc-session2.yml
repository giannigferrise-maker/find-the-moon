name: SDLC Session 2 — Code Implementation

on:
  issues:
    types: [labeled]

jobs:
  code-implementation:
    if: github.event.label.name == 'code-ready'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install anthropic

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create branch
        run: |
          BRANCH="sdlc/issue-${{ github.event.issue.number }}-code"
          git checkout -b "$BRANCH"
          echo "BRANCH=$BRANCH" >> $GITHUB_ENV

      - name: Run SDLC Session 2
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: python3 .github/scripts/sdlc_session2.py

      - name: Commit and push
        run: |
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "SDLC Session 2: code implementation for issue #${{ github.event.issue.number }}"
            git push origin "$BRANCH"
          fi

      - name: Create PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if git ls-remote --exit-code origin "$BRANCH" > /dev/null 2>&1; then
            gh pr create \
              --title "SDLC: Code implementation for issue #${{ github.event.issue.number }}" \
              --body "$(cat .github/sdlc_pr_body.md)" \
              --base main \
              --head "$BRANCH"
          else
            echo "No changes were pushed — skipping PR creation."
          fi
